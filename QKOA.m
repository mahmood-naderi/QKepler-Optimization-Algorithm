% Quentum Kepler Optimization Algorithm
function [Sun_Score, Sun_Pose, Convergence_curve] = QKOA(SearchAgents_no, Tmax, ub, lb, dim, fobj, fhd)

% Definitions
Sun_Pos = zeros(1, dim)
Sun_Score = inf;
Convergence_curve = zeros(1, Tmax);

% Controlling Parameters
Tc = 3;
M0 = 0.1;
lambda = 15;

% Initialization

end %% End Funciton
